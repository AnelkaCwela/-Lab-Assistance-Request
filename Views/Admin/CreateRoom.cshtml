@model RoomModel
@{
    ViewData["Title"] = "Register Room";
}
<h4 class="text-center">Register Room</h4>
<hr />
@if (ViewBag.Data != null)
{
    <h4 class="text-center alert alert-success alert-heading"> @ViewBag.Data</h4>
}
<form asp-action="CreateRoom" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row mb-4">
        <div class="col">
            <div class="form-outline">
                <label class="form-label" for="form3Example1">Room Name</label>
                <input asp-for="RoomName" placeholder="Room Name" required id="form3Example1" class="form-control" />

                <span asp-validation-for="RoomName" class="text-danger"></span>
            </div>
        </div>
    </div>
    <!-- Submit button -->
    <button type="submit" class="btn btn-primary btn-block mb-4">Submit</button>
</form>